# Structured data for the experiment-timelapses topics page.
#
# `copy_id` maps to locale keys (experiment_timelapses_<copy_id>_title / _desc)
# so all user-facing copy stays centralized in _data/locales/*.yml.
# `sensor` and `duration` are shown as technical metadata.
# `c1`/`c2` are placeholder gradient colors for the demo thumbnail SVG; replace
# the SVG block in experiment-timelapses-content.html with a real <video>/image
# element when you have actual assets.

- copy_id: "item_1"
  sensor: CBERS-4A
  duration: "00:32"
  c1: "#0B1526"
  c2: "#2C5E8C"

- copy_id: "item_2"
  sensor: CBERS-4A
  duration: "00:47"
  c1: "#221A45"
  c2: "#7A3E6E"

- copy_id: "item_3"
  sensor: Amazonia-1
  duration: "01:08"
  c1: "#0D2818"
  c2: "#1E6B4F"

- copy_id: "item_4"
  sensor: CBERS-4
  duration: "00:53"
  c1: "#10131F"
  c2: "#6E2C2C"
